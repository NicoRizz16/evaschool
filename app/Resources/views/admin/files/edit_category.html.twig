{% extends 'admin/base.html.twig' %}

{% block body %}

    <div class="jumbotron">
        <h4 class="main-title text-center"> Modifier la catégorie "{{ category.name }}" </h4>
        <hr class="my-2">

        <div class="col-md-6 offset-md-3 mb-5 pt-5 pb-5">

            {{ form_start(form) }}

            <div class="md-form">
                {{ form_row(form.name) }}
            </div>
            <br>
            <div class="md-form">
                {{ form_row(form.color) }}
            </div>
            <br>
            <div class="text-center">
                <button type="submit" class="btn btn-amber btn-rounded btn-md" formnovalidate>Enregistrer</button>
                <a href="{{ path('admin_files_list_by_section_category', {'section': section, 'category_id': parent_category_id}) }}" class="btn btn-outline-danger btn-rounded waves-effect btn-sm">Annuler <i class="fa fa-times ml-1"></i></a>
            </div>

            {{ form_end(form) }}

        </div>
    </div>

{% endblock %}
